
<script setup>
import { ref } from "vue";

    const canvas = ref(null);

    onMounted(() => {
        
    });


</script>

<template>
    <canvas ref="site-title-canvas" id="site-title-canvas" :key="canv"></canvas>
</template>